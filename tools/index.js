const getFolderFiles = require("./getFolderFiles");
const copyDirectory = require("./copyDirectory");
const removeDirectory = require("./removeDirectory");

module.exports = {
  getFolderFiles,
  copyDirectory,
  removeDirectory
};
